# next-auth@5 is still in beta and declares overly-strict peer deps;
# @auth/drizzle-adapter does the same. Without this flag, npm refuses
# to install the lockfile that actually works at runtime. Once next-auth
# ships a stable v5 you can drop this line.
legacy-peer-deps=true
